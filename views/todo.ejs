<!DOCTYPE html>
    <head>
        <title>Checkmate</title>
        <link rel="stylesheet" href="/static/stylesheets/style.css" type="text/css">
        <link href="https://fonts.googleapis.com/css?family=Baloo+Bhai|Candal|Chewy&display=swap" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css2?family=Comfortaa:wght@600&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.2/css/all.css"
        integrity="sha384-oS3vJWv+0UjzBfQzYUhtDYW+Pj2yciDJxpsK1OYPAYjqT085Qq/1cq5FLXAZQ7Ay" crossorigin="anonymous">
    </head>
    <body>

<!--------   Nav/Hamburger Menu --(STYLESHEET REF= Style 1)--------->
<div class="home-header">
    <div class="banner">
    <nav role="navigation">
        <div id="menuToggle">
        <input type="checkbox" />
            <span></span>
            <span></span>
            <span></span>
        
        <ul id="menu">
            <a href="/"><li>Home</li></a>
            <a href="/about"><li>About</li></a>
            <a href="/"><li>My Lists</li></a>
            <a href="login.html"><li>Login</li></a>
        </ul>
        </div>
    </div>
</div>
        <div class="todo-container">
            <h2>My list</h2>
         <div class="todo">
            <form action="/" method="POST" class="todo-header">
                <input type="text" name="content" placeholder=" add">
                <button type="submit"><span class="fas fa-plus"></span></button>
            </form>
            <ul class="todo-list">
                <% todoTasks.forEach(details => { %>
                <li class="todo-list-item">
                   <% if (details.completed){%>
                    <div class="todo-list-item-name strikethrough"><%= details.content %></div>
                  
                    <% }else {%>
                        <div class="todo-list-item-name"><%= details.content %></div>
                    <%} %>
                   
                <a href="/edit/<%= details._id %>" class="edit">
                <span class="fas fa-edit"></span>
                </a>
                <a href="/remove/<%= details._id %>" class="remove">
                <span class="fas fa-times"></span>
                <% if (details.completed){%>
                <a href="/complete/<%= details._id %>" class="done">
                    <span class="fas fa-check done"></span>
                    <% }else {%>
                        <a href="/complete/<%= details._id %>" class="done">
                            <span class="fas fa-check"></span>
                        <%} %>
                </a>
                </li>
                <% }) %>
                </ul>
        </div>
    </div>
</body>